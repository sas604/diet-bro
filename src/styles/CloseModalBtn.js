import styled from 'styled-components';

export const CloseModalBtn = styled.button`
  float: right;
  top: 6px;
  right: 0;
  line-height: 1.3;
  font-size: 1.5rem;
  appearance: none;
  border: none;
  background: transparent;
  &:hover {
    color: var(--red);
  }
`;
